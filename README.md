# a2

Repository for the `a2` C++ source files.

Includes `2.cpp`.

## How to push

Run:

```powershell
git push -u origin main
```

You may be prompted for GitHub credentials (use a Personal Access Token).
